# 313flight

AI-powered flight search with flexible dates and a book-or-wait advisor.

## Local setup

```bash
npm install
cp .env.example .env   # then edit
npm start
```

The app runs on http://localhost:4000
